## Norish (Runtipi)

Norish draait met:
- Postgres (persistent)
- Redis (persistent)
- Headless Chrome (voor browser-automations)

Password auth staat aan (`PASSWORD_AUTH_ENABLED=true`).

Tip: expose de app via Runtipi als je een vaste domain wilt gebruiken.
